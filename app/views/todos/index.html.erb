<main class="TodoPageMain">
    <article>
      <h1 class="TodoPage-title">My To-do List</h1>
      <%= form_with(url: "/todos", method: "POST",class: "AddTodo") do %>
        <%= text_field_tag("todo_text","",placeholder: "what's next?",class: "AddTodo-text")%>
        <%= date_field_tag("due_date",nil,class: "AddTodo-date")%>
        <%= button_tag("Add",type: "submit",class: "AddTodo-button")%>
      <% end %>
      <%= render partial: "todo-section", locals: {
        section_title: "Overdue", todos: Todo.overdue, show_dueDate: true
        } %>
      <%= render partial: "todo-section", locals: {
        section_title: "Due Today", todos: Todo.due_today, show_dueDate: false
        } %>
      <%= render partial: "todo-section", locals: {
        section_title: "Due Later", todos: Todo.due_later, show_dueDate: true
        } %>
    </article>
  </main>
